# Import package
import matplotlib.pyplot as plt 

# Build Scatter plot
# os valores já estavam disponíveis na plataforma, então não foi necessário declarar, apenas colocar na função.
plt.scatter(pop, life_exp)

# Show plot
plt.show()
